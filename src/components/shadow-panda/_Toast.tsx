'use client';

import { styled } from '@styled-system/jsx/factory.mjs';
import * as ToastPrimitive from '@radix-ui/react-toast';

export const __Toast = styled(ToastPrimitive.Root);
export const _ToastAction = styled(ToastPrimitive.Action);
export const _ToastClose = styled(ToastPrimitive.Close);
export const _ToastTitle = styled(ToastPrimitive.Title);
export const _ToastDescription = styled(ToastPrimitive.Description);
